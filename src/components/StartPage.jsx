import React from "react";

const StartPage = () => {
  return (
    <section className="startBackground">
      <div className="startContainer">
        <section className="startPageContainer">
          <h1 className="startPageMessage">langsnap</h1>
        </section>
      </div>
    </section>
  );
};

export default StartPage;
